import {createQueryKeys} from "@lukemorales/query-key-factory";

export const consultationRequestQueryKeys = createQueryKeys(
	"consultation-request",
	{
		"get-all-consultation-requests": null,
	},
);
